# 开发环境配置
# 默认不需要设置 VITE_API_URL，Vite proxy 会自动代理 /api 到后端
# 如果需要直连后端（不经过 Vite proxy），可设置为绝对地址：
# VITE_API_URL=http://localhost:3000/api

# Vite proxy 后端目标地址（默认 http://localhost:3000）
# VITE_API_TARGET=http://localhost:3000
