# API 基础地址配置示例
# 默认不需要设置，前端会自动使用相对路径 /api（通过 Vite proxy 或同端口托管）

# 如需指定 API 地址（直连后端），取消注释并修改：
# VITE_API_URL=http://localhost:3000/api

# Vite dev server proxy 后端目标地址（默认 http://localhost:3000）
# VITE_API_TARGET=http://localhost:3000
